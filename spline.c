#include <stdio.h>
#include "spline.h"
#include <stdlib.h>
#include <math.h>

int main() {
 
  int i,j,k;
  FILE *fp;
  double x,y,z;
  
  fp = fopen("input.txt","r");
  
  if(fp == NULL) { printf("error open file\n"); exit(1); }

  
  
  return 0;
}
